# Step 19F - Rollback

Added a guarded Container App rollback script and manually approved GitHub
Actions rollback workflow.

Rollback requires an existing immutable ACR tag, exact confirmation, and a
successful Container App health check.
